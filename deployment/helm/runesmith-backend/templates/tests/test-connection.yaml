apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "runesmith-backend.fullname" . }}-test-connection"
  labels:
    {{- include "runesmith-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "runesmith-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
