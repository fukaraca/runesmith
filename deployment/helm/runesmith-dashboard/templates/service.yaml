apiVersion: v1
kind: Service
metadata:
  name: {{ include "runesmith-dashboard.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: runesmith-dashboard
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
