apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "runesmith-operator.fullname" . }}-test-connection"
  labels:
    {{- include "runesmith-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "runesmith-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
