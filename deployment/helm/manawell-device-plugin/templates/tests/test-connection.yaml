apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "manawell-device-plugin.fullname" . }}-test-connection"
  labels:
    {{- include "manawell-device-plugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "manawell-device-plugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
